
# * กำหนดค่าของตัวแปร value เป็น 45
value = 45

# * ใช้ฟังก์ชัน format() เพื่อแปลงค่า value เป็นสตริงที่แสดงเลขฐานสอง (binary)
binary_value = format(value, 'b')

# * แสดงค่า binary_value ที่เป็นผลลัพธ์จากการแปลง
print(binary_value)